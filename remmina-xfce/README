To compile in Debian, please install the following package:

libxfce4util-dev xfce4-panel-dev libavahi-client-dev

To compile and install remmina-xfce package, please execute the following command as root:

$ ./configure
$ make
$ make install

NOTE: This package requires to be installed in the same prefix as remmina package.

